# 本地使用说明

1. 在 Windows 电脑上安装 Node.js 22 或更高版本。
2. 解压整个网站文件夹。
3. 双击 `start-local.bat`。启动器会关闭遗留的旧版学习室进程，固定使用 4173 端口，并直接运行在当前 Windows 电脑上构建的网站成品；同时会自动修复 Vinext 0.0.50 在 Windows 上无法读取嵌套静态资源的路径兼容问题，确认版本、页面、样式和客户端脚本均正常后才打开浏览器。
4. 首次运行会安装所需组件；只要继续在同一个文件夹更新和使用，以后不会重复下载，也不会在日常启动时重新编译页面。
5. 使用完毕后可直接关闭启动窗口；如果窗口已经关闭但网站进程仍在，可双击 `stop-local.bat`。
6. 进入“条文库”后点击“选择PDF”，导入你持有的《公司法》英文版。网站将在本机识别并保存英文条文，不会上传该文件。
7. 如要启用普通词语的精准中英对应，点击阅读区右上角“启用精准对齐”。Chrome 或 Edge 会首次下载浏览器自带的中英模型；下载完成后，翻译和对齐都在本机运行，并可离线继续使用。法律术语仍优先采用网站内置的公司法术语表。

学习进度、收藏、笔记和上次阅读位置保存在当前浏览器中。请在“学习记录”页面定期导出备份文件。更换浏览器或清理浏览器数据前，也应先导出备份。

中文《公司法》为权威文本。英文内容取自用户提供的参考译本，仅用于法律英语学习和中英对照。

## v3.1.4 启动修复说明

v3.1.3 的 Windows 本机构建本身成功，但 Vinext 0.0.50 在 Windows 上创建静态文件缓存时保留了反斜杠路径，导致页面引用的 `/assets/*.css` 和 `/assets/*.js` 返回 404。v3.1.4 在启动前进行一次幂等兼容修复，并在启动失败时把最后一个未通过的验证环节写入 `startup-log.txt`。这一修复不读取、修改或清除浏览器中的学习数据。
